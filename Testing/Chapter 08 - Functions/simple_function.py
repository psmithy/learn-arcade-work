# Add two numbers and return the results
def sum_two_numbers(a, b):
    result = a + b
    return result

# Capture the result. If this isn't done, "result" only exists inside the function!
my_result = sum_two_numbers(7, 11)

# Print the captured result
print(my_result)